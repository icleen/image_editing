cmake_minimum_required(VERSION 2.8)
project( SeamCarver )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( SeamCarver seamcarver.cpp )
add_executable( EnergyFinder energy_finder.cpp )
target_link_libraries( SeamCarver ${OpenCV_LIBS} )
